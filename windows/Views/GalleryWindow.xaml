<Window x:Class="Palantir.Views.GalleryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Live Wallpaper"
        Width="720" Height="560"
        MinWidth="500" MinHeight="400"
        WindowStartupLocation="CenterScreen"
        Background="#1E1E1E"
        FontFamily="Segoe UI"
        ShowInTaskbar="True">

    <Window.Resources>
        <Style x:Key="HeaderButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#404040"/>
            <Setter Property="Foreground" Value="#E0E0E0"/>
            <Setter Property="BorderBrush" Value="#555555"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#555555"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#333333"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#2D2D2D" Padding="16,12">
            <DockPanel>
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Left" VerticalAlignment="Center">
                    <TextBlock Text="&#xE714;" FontFamily="Segoe MDL2 Assets" FontSize="20"
                               Foreground="#4FC3F7" VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <TextBlock Text="Live Wallpaper" FontSize="18" FontWeight="SemiBold"
                               Foreground="#E0E0E0" VerticalAlignment="Center"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" DockPanel.Dock="Right" HorizontalAlignment="Right">
                    <Button x:Name="RefreshButton" Margin="0,0,8,0"
                            Click="RefreshButton_Click" Style="{StaticResource HeaderButtonStyle}">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="&#xE72C;" FontFamily="Segoe MDL2 Assets" Margin="0,0,4,0"/>
                            <TextBlock Text="Refresh"/>
                        </StackPanel>
                    </Button>
                    <Button x:Name="OpenFolderButton"
                            Click="OpenFolderButton_Click" Style="{StaticResource HeaderButtonStyle}">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="&#xE838;" FontFamily="Segoe MDL2 Assets" Margin="0,0,4,0"/>
                            <TextBlock Text="Open Folder"/>
                        </StackPanel>
                    </Button>
                </StackPanel>
            </DockPanel>
        </Border>

        <!-- Separator -->
        <Border Grid.Row="1" Height="1" Background="#404040"/>

        <!-- Gallery Grid -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Disabled">
            <ItemsControl x:Name="WallpaperGrid" Padding="16">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel ItemWidth="220" ItemHeight="195"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
            </ItemsControl>
        </ScrollViewer>

        <!-- Status Bar -->
        <Border x:Name="StatusBar" Grid.Row="3" Background="#2D2D2D"
                Padding="12,8" Visibility="Collapsed"
                BorderBrush="#404040" BorderThickness="0,1,0,0">
            <DockPanel>
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Left" VerticalAlignment="Center">
                    <TextBlock x:Name="DesktopStatusText" Foreground="#4CAF50" FontSize="11"
                               VerticalAlignment="Center" Margin="0,0,16,0"/>
                    <TextBlock x:Name="LockStatusText" Foreground="#42A5F5" FontSize="11"
                               VerticalAlignment="Center"/>
                </StackPanel>

                <Button x:Name="StopButton" DockPanel.Dock="Right" HorizontalAlignment="Right"
                        Click="StopButton_Click" Style="{StaticResource HeaderButtonStyle}">
                    <TextBlock Text="Stop Desktop" FontSize="11"/>
                </Button>
            </DockPanel>
        </Border>
    </Grid>
</Window>
